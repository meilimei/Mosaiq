# ─────────────────────────────────
# Node / npm / pnpm / yarn
# ─────────────────────────────────
node_modules/
.pnpm-store/
.yarn/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ─────────────────────────────────
# Rust / Cargo
# ─────────────────────────────────
target/
**/*.rs.bk
Cargo.lock
*.pdb

# ─────────────────────────────────
# C++ / Chromium fork（chromium-fork/ 子仓库内）
# ─────────────────────────────────
chromium-fork/src/
chromium-fork/depot_tools/
chromium-fork/out/
chromium-fork/.gclient
chromium-fork/.gclient_entries
chromium-fork/.cipd/
*.o
*.obj
*.so
*.dylib
*.dll
*.lib
*.a
compile_commands.json
*.gn.gen
*.ninja
*.ninja.d
*.ninja_log
*.ninja_deps

# ─────────────────────────────────
# Build outputs
# ─────────────────────────────────
dist/
dist-electron/
dist-tsc/
build/
out/
# Generated Detection Lab leaderboard (built fresh by scripts/build-leaderboard.mts / CI).
site/
.next/
.nuxt/
.svelte-kit/
*.exe
*.dmg
*.deb
*.rpm
*.AppImage
*.msi

# ─────────────────────────────────
# OS / IDE
# ─────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.cursor/
*.suo
*.user

# ─────────────────────────────────
# Secrets
# ─────────────────────────────────
.env
.env.local
.env.*.local
.env.cloud
*.pem
*.key
*.p12
*.cer
secrets/
!.env.example
!.env.cloud.example
# cloud-runtime sqlite + browser-pod chromium 持久化目录（dev 跑 docker compose 之外的本地路径）
apps/cloud-runtime/data/
apps/browser-pod/data/
# 本地 e2e 验收时存 pod / runtime pid、log、sqlite、chromium user-data-dir
tmp/

# ─────────────────────────────────
# Test / coverage
# ─────────────────────────────────
coverage/
.nyc_output/
*.lcov
.vitest-cache/

# ─────────────────────────────────
# Persona library（敏感 + 巨大）
# ─────────────────────────────────
data/persona/*.bin
data/persona-raw/
*.persona-pack

# ─────────────────────────────────
# Detection Lab 缓存
# ─────────────────────────────────
data/detection-cache/

# ─────────────────────────────────
# Tauri / Desktop runtime
# ─────────────────────────────────
apps/desktop/src-tauri/target/
apps/desktop/src-tauri/gen/
apps/desktop/dist/
apps/desktop/.tauri/

# Mosaiq Desktop 运行时数据（每个 persona 的浏览器数据目录）
runtime/profiles/
runtime/cache/

# Biome / ESLint 缓存
.biome-cache/
.eslintcache

# ────────────────────────────────────────
# SDK baseline detection bench 输出
# ────────────────────────────────────────
# 每次跑会生成几 MB 截图 + HTML，不入库
packages/sdk/bench/results/

# ────────────────────────────────────────
# Smoke test JSONL traces（stagehand-compat-smoke 等）
# ────────────────────────────────────────
# 每次 prod smoke 跑会留一个 JSONL，含 sessionId 时序，用于 §6.1 表填表后归档；
# 实测数据已写进文档，原始文件留本地参考、不入库。
.smoke-results/

# tshy 临时输出
.tshy-build-tmp/
